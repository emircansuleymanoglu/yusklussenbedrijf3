Options -MultiViews
RewriteEngine On
RewriteBase /

# API PHP bestanden direct doorsturen
RewriteRule ^api/ - [L]

# Bestaand bestand of map direct serveren
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Al het andere naar React sturen
RewriteRule ^ index.html [L]
